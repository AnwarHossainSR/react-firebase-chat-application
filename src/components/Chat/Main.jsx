import { Stack } from '@mui/material';

const Main = () => {
  return (
    <Stack
      sx={{
        background: '#262E35',
        minHeight: '100vh',
        maxHeight: '100vh',
      }}
    >
      Main
    </Stack>
  );
};

export default Main;
